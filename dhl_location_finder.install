<?php

/**
 * Implements hook_install().
 */
function dhl_location_finder_install() {
    \Drupal::configFactory()->getEditable('dhl_location_finder.settings')
        ->set('dhl_api_key', '')
        ->set('dhl_api_base_url', '')
        ->save();
}
